package constants

object Constants {
  val urlPath = "jobcoin.url"
  val transactionsEndpointPath = "jobcoin.transactions-endpoint"
  val addressesEndpointPath = "jobcoin.addresses-endpoint"
  val jobcoinRetries = "jobcoin.retries"
  val jobcoinTimeout = "jobcoin.timeout"
  val jobcoinConfig = "jobcoinConfig"
}
